# Vaultwarden

The `ADMIN_TOKEN` is a token generated by running `openssl rand -base64 48`. This token is needed every time you access [the admin panel](https://vault.ha.home.woutvdb.be/admin).

> [!IMPORTANT]
> Don't forget to set the `SIGNUPS_ALLOWED` environment variable to false when everyone is registered
